# Written by Adam, just ideas to implement/clarify

- ask for a code walkthrough

- define the user journey to clean up the UI/UX
    - move analyze a channel input
    - move saved analyses
    - move "compare" channels
    - basically move the operations somewhere -> sidebar sounds nice here
    - brainstorm solutions here

    - brainstorming:
    Help me create a plan for redesigning the UI.
If there are no analyses yet, I want to have a simple search bar and a little bit of highly engaging, short copy above. One sentence max, plus one sentence subtext if necessa
ry. When the "analyze" button is clicked, it should analyze some videos just like before. When the analysis is done, the analysis should open. This could be a new view. There
 should be a place where we see the list of analyses, we can delete them and the search bar + button to add new analyses should also be here. Sort of a control center for ana
lyses. When clicking on an analysis card there, users should see the analysis detail view: a familiar video card view just like YouTube has, like we have it right now. Scroll
ing down the "load more" should be seen (or we could also load all analyzed videos at once..). Clicking on a video card should open it in a new view so we can see the details
 better, and also run a transcript analysis on it. I also want a page where you can input a video url and get back the transcript analysis. We could somehow merge the 2 UIs?
not sure.

- LLM analysis timestamps are weird

- try to correlate views/engagement/outlier score with video duration, maybe also upload date
- try to correlate video topics with duration, upload date (seasonality)
- correlate thumbnail to scores
- ability to search
- ability to sort according to any metric attached to a video (upload date, duration, likes, views,
outlier score, etc.)

- PUBLISH
